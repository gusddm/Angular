import { ItemDetailDirective } from './item-detail.directive';

describe('ItemDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemDetailDirective();
    expect(directive).toBeTruthy();
  });
});
